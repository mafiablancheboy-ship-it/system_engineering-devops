#!/usr/bin/env bash
# This script lists non-hidden files and prints only the part after the first dash

for file in *; do
    # ignore hidden files
    [[ "$file" == .* ]] && continue

    # print everything after the first '-'
    echo "${file#*-}"
done
